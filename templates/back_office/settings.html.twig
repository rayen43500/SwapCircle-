{% extends 'back-base.html.twig' %}

{% block title %}Paramètres - SwapCircle{% endblock %}

{% block body %}
<div class="app-content pt-3 p-md-3 p-lg-4">
    <div class="container-xl">
        <div class="row g-3 mb-4 align-items-center justify-content-between">
            <div class="col-auto">
                <h1 class="app-page-title mb-0">
                    <i class="fas fa-cog text-warning me-2"></i>
                    Paramètres Système
                </h1>
                <p class="text-muted mt-1">Configurez l'application pour une meilleure expérience utilisateur ♻️</p>
            </div>
        </div>

        <div class="row g-4">
            <div class="col-12">
                <div class="app-card app-card-settings h-100 shadow-sm">
                    <div class="app-card-header p-3 border-bottom-0">
                        <h4 class="app-card-title mb-0">
                            <i class="fas fa-sliders-h text-info me-2"></i>
                            Configuration en cours de développement
                        </h4>
                    </div>
                    <div class="app-card-body p-3 text-center">
                        <i class="fas fa-wrench fa-3x text-muted mb-3"></i>
                        <p class="text-muted">Les paramètres système seront bientôt disponibles.</p>
                        <p class="text-muted">Cette section permettra de configurer les notifications, les thèmes, et les préférences générales.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}